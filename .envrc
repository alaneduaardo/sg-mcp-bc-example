# direnv config: load local dev config from .env when present.
# Copy .env.example → .env, fill it in, then run `direnv allow` once.
#
# This is a developer-machine convenience only. The server binary reads solely
# the process environment (12-factor); it never loads .env itself. In CI/CD the
# variables are injected by the platform.
dotenv_if_exists
